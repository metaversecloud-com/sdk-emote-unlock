apiVersion: v1
kind: Service
metadata:
  name: emunlock0
  labels:
    app.kubernetes.io/name: emunlock0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: emunlock0
  ports:
    - name: http
      port: 80
      targetPort: http
